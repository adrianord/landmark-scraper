export type Config = {
  baseURL: string,
  propertyId: string,
  interval: string
}

export const config: Config = {
  baseURL: "https://www.landmarkatgleneagles.com/availableunits.aspx",
  propertyId: "648548",
  interval: "1d"
}
